"""Command-line entry point for testing one gateway request.

This script will eventually load environment variables, accept a prompt from
the command line or standard input, send it through the gateway, and print the
response plus any retry or failover details.
"""

